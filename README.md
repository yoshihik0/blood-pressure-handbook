# 血圧手帳 - Smart BP Assistant

PCやスマートフォンのブラウザで簡単・軽快に血圧を記録・管理できるWebアプリケーションです。

`index.html` をブラウザで直接開くだけで今すぐ使い始めることができます。

👉 **[血圧手帳アプリをブラウザで開く（GitHub Pages）](https://yoshihik0.github.io/blood-pressure-handbook/)**

---

## 📸 主な機能と特徴

### 1. テンキーによるスマート自動入力
- 画面上のテンキーで **最高血圧（3桁）** ➔ **最低血圧（2桁）** ➔ **脈拍（2桁）** と入力するだけで、入力箇所が自動で進みます。
- 脈拍の入力が完了すると自動的に保存処理が実行されます。

### 2. 日本高血圧学会（JSH）基準に準拠した判定
- 入力された数値から「最適血圧」「正常血圧」「I度高血圧」などをリアルタイムで自動分類し、色分けされたバッジで表示します。

### 3. データのCSV保存と復元（インポート・エクスポート）
- データはブラウザに保存されます。
- 記録したデータは、Excel等でも開きやすいBOM付きUTF-8のCSV形式でダウンロードできます。
- エクスポートしたCSVファイルを読み込む（インポートする）ことで、別ブラウザや別端末からでもデータを引き継ぐことができます（既存データとの重複は自動で排除・マージされます）。

### 4. 推移グラフと分布分析
- **推移グラフ**: 直近10回分の血圧（最高・最低）の傾向を折れ線グラフで時系列表示します。
- **割合分析**: これまでの測定データの判定分類比率をパーセンテージで可視化します。

---

## 📂 CSVインポート用のファイル書式（書き方）

手動で作成した血圧データファイル（テキストやCSV）をアプリに読み込ませる場合の書式説明です。

### 基本ルール
1. **項目の順番**: `測定日時,最高血圧,最低血圧,脈拍,メモ` の順にカンマ `,` で区切って記述します。
2. **ID列は不要**: 各データのIDはアプリ読み込み時に自動で生成されます。
3. **ヘッダー（見出し行）は自由**: 1行目に項目名の見出しがあっても無くても、アプリが自動で判別して正しく取り込みます。
4. **メモの省略**: メモがない場合は、最後のカンマや項目を省くことができます。

### ✏️ 記入例（架空のデモデータ）

#### パターンA：ヘッダー（見出し行）がある場合
```csv
測定日時,最高血圧,最低血圧,脈拍,メモ
2026/04/01 08:30,118,76,64,朝の測定
2026/04/01 21:15,124,81,68,
2026/04/02 08:00,121,79,66
```

#### パターンB：ヘッダーがなく、データのみの場合（最もシンプル）
```csv
2026/04/01 08:30,118,76,64,朝の測定
2026/04/01 21:15,124,81,68
2026/04/02 08:00,121,79,66
```
※いずれのファイルも、ファイルの拡張子を `.csv` または `.txt` にして保存してください。

---

## 🛠 使い方

### 1. 動かし方
- **Web上**: [GitHub Pages](https://yoshihik0.github.io/blood-pressure-handbook/) にアクセスするだけで使えます。
- ブラウザの「ホーム画面に追加」でアプリ的に使うことができます。

### 2. データの保存先
- 入力したデータは、ブラウザ内の `localStorage`（ローカルストレージ）に保存されます。
- ページを閉じたり、ブラウザをリロードしてもデータは消えません。
- ※ブラウザのキャッシュや履歴を完全に削除（初期化）するとデータが消えるため、定期的なCSV保存を推奨します。

---

## 📂 ファイル構成

- `index.html` : アプリケーション本体（HTML/CSS/JSのすべてを1ファイルに統合。キャッシュによる読み込み遅延・崩れを防ぎます）
- `README.md` : このドキュメント
